---
'nunjitsu': patch
---

Preserve nested-loop compiler slots and length temporaries across repeated entries while keeping multi-target array and record branch mappings distinct and bounding duplicated branch planning with the work limit.
