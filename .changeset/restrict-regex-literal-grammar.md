---
"nunjitsu": patch
---

Restrict regex literals to the fixed Nunjucks `gimy` flag grammar, synchronize
tag-scanner identifier boundaries, and reject ambiguous delimiter escapes
before template capabilities can execute.
