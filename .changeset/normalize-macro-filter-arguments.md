---
'nunjitsu': patch
---

Normalize `int` and `sort` arguments through Nunjucks-compatible macro binding so occupied positional parameters override same-named keywords without discarding callable authority.
