---
'nunjitsu': patch
---

Preserve inherited compiler-slot precedence when a synthetic caller declares a same-named defaulted parameter, preventing caller defaults from redirecting capability dispatch.
