{
  "name": "nunjitsu",
  "version": "0.0.0",
  "description": "A secure direct string template engine written in TypeScript",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.12.0"
  },
  "main": "./dist/cjs/index.cjs",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.cjs"
    }
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "benchmark": "node benchmarks/run.ts",
    "benchmark:quick": "node benchmarks/run.ts --warmup=1 --iterations=3",
    "build": "npm run clean && npm run build:typescript && node scripts/finalize-build.mjs",
    "build:typescript": "tsgo -p tsconfig.esm.json && tsgo -p tsconfig.cjs.json && node scripts/build-cjs.mjs",
    "clean": "node scripts/clean.mjs",
    "test": "npm run typecheck && npm run test:source && npm run test:benchmark && npm run test:package",
    "test:benchmark": "node benchmarks/run.ts --warmup=0 --iterations=1 --json",
    "test:package": "npm run build && node --test tests/package/esm.test.mjs tests/package/commonjs.test.cjs",
    "test:source": "node --test tests/source/*.test.ts",
    "typecheck": "tsgo -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/nunjucks": "3.2.6",
    "@typescript/native-preview": "7.0.0-dev.20260613.1",
    "esbuild": "^0.25.0",
    "nunjucks": "3.2.4"
  }
}
